{
  "build": {
    "env": {
      "PYTHON_VERSION": "3.9",
      "NPM_CONFIG_BUILD_FROM_SOURCE": "false",
      "SQLITE3_DISABLE_PREBUILT": "false"
    },
    "scripts": {
      "preinstall": "apt-get update && apt-get install -y python3-distutils"
    }
  }
}
